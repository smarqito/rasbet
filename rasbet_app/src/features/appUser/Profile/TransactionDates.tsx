import { observer } from 'mobx-react-lite'
import React from 'react'

const TransactionDates: React.FC = () => {
  return (
    <div>TransactionDates</div>
  )
}

export default observer(TransactionDates)